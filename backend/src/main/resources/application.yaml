spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/DISS_DB
    username: postgres
    password: 123Andrei
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  
    show-sql: true      
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    