# DISS_Project

How to initialize the DB:

Install Docker Desktop if you don't have it.
Run it: Open your terminal in the backend folder and run:
docker-compose up -d

Then start the backend and the db will be stored in the docker container.
